import React from 'react';
import styles from './Filters.module.css';

export default function Filters() {
  return (
    <div className={styles.filtersContainer}>
        <h4 className={styles.filtersTitel}>Фильтры</h4>
    </div>
  )
}
